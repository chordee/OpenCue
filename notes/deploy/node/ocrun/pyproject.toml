[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ocrun"
version = "0.1.0"
description = "Run a DCC program on an OpenCue render node"
requires-python = ">=3.11"

[project.scripts]
ocrun = "ocrun:main"

[tool.setuptools]
py-modules = ["ocrun"]
